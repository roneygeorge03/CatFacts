Cat facts API is implemented

Logics have developed for Get allfact and get a random fact

For getting a random fact input a random integer in swagger in the Get endpoint.

For gettting all fact hit on Get endpoint.

Add new fact to existing fact list is also implemented  by adding item to json converterd list and Serializing json.

For adding click on Post end point.

if I could get more time i need to add swagger UI more interative by adding comments 
and need to optimise the code and improve performace by running all test case